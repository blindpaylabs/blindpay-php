{
  "$schema": "Recorded, reasoned, owned divergences that state reconciliation must not silently paper over. Two kinds: `enumValues` is a spec enum member whose value does not match any SDK case value even though the SDK models that enum (usually because an SDK case value has a typo/format bug) -- the fix is to CORRECT the existing case, not to add a near-duplicate new one, so scripts/api-sync.php treats a listed (enum, specValue) pair as satisfied rather than pending drift. `fields` covers two situations, both keyed by {schema, field}: (a) a spec property backed by a constrained enum but modeled as a plain untyped field in the SDK, so there is no case-completeness gap to begin with; (b) a genuine type-representation mismatch between the spec's declared type and the SDK's declared PHP property type on an already-modeled field, recorded here rather than silently accepted or blindly auto-fixed, since correcting it is a deliberate code change a human must make. Distinct from .api-sync/unmodeled.json, which is for properties absent from the SDK outright.",
  "enumValues": [
    {
      "enum": "BankAccountType",
      "specValue": null,
      "sdkCase": "TED",
      "sdkValue": "ted",
      "reason": "SDK-only case with no counterpart in the spec's account_type enum (which only has checking/saving). Pre-existing, unrelated to account_type modeling; not touched here.",
      "owner": "eric@blindpay.com"
    }
  ],
  "fields": [
    {
      "schema": "CustomerOut",
      "field": "kyc_status",
      "reason": "Spec constrains kyc_status to an 8-value enum (verifying/approved/rejected/deprecated/pending_review/awaiting_contract/compliance_request/approved_rfi). This SDK models it as a plain `string` on BaseCustomer, not a backed enum, so every spec value already parses without error -- there is no case-completeness gap in code today. Recorded for visibility only.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "QuoteOut",
      "field": "expires_at",
      "reason": "TYPE MISMATCH, pre-existing and identical between the last two spec snapshots (not new drift): spec declares expires_at as `number|null` (a float), CreateQuoteResponse declares `public int $expiresAt` with no cast in fromArray. In practice this is a Unix timestamp (spec example: 1712958191, always a whole number), so json_decode already hands back a PHP int and the mismatch has not surfaced -- but a genuinely fractional or null value from the API would throw a TypeError under strict_types. Not fixed here: changing the property type is a deliberate decision, not a mechanical field add.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "CreatePayinQuoteOut",
      "field": "expires_at",
      "reason": "Same pre-existing mismatch as QuoteOut.expires_at, on the payin-quote equivalent (CreatePayinQuoteResponse). Here the field IS cast, `expiresAt: (int) $data['expires_at']`, so a fractional value would silently truncate rather than throw -- a real risk in disguise if the API ever returns one.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "BankAccountOut",
      "field": "spei_protocol",
      "reason": "Spec constrains spei_protocol to an enum (clabe/debitcard/phonenum). BankAccountListItem models it as a plain `?string`, not the SpeiProtocol backed enum that CreateSpeiInput/CreateSpeiResponse already use on the same rail -- so every spec value still parses without error, just without case-completeness checking on this shape. Recorded for visibility only.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayinOut",
      "field": "status",
      "reason": "Covers PayinOut/CreatePayinOut's inline tracking_transaction.status (failed/completed) and tracking_transaction.status is a DIFFERENT, smaller enum than PayinOut's own top-level status (TransactionStatus, already mapped). TrackingTransaction models this sub-field as a plain `string`, not a backed enum.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayinOut",
      "field": "currency",
      "reason": "Spec constrains currency to the Currency enum. Payin models it as a plain `string`; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayinOut",
      "field": "payment_method",
      "reason": "Spec constrains payment_method to the PaymentMethod enum (already mapped for other shapes). Payin models it as a plain `string`, not PaymentMethod; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayinOut",
      "field": "pse_document_type",
      "reason": "Spec constrains pse_document_type to a 2-value enum (CC/NIT). Payin models it as a plain `?string`; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayinOut",
      "field": "type",
      "reason": "Spec constrains type (deposit method) to an enum. Payin models it as a plain `string`; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "OfframpWallet",
      "field": "network",
      "reason": "Spec constrains network to the Network enum (already mapped for other shapes, e.g. WalletOut). OfframpWallet models it as a plain `string`, not Network; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayoutOut",
      "field": "status",
      "reason": "Covers PayoutOut/PayoutOnEvmOut's inline tracking_transaction.status (failed/found) and tracking_complete.status (tokens_refunded/paid) -- two DIFFERENT, smaller enums than PayoutOut's own top-level status (TransactionStatus, already mapped). TrackingTransaction/TrackingComplete model these sub-fields as plain `string`, not backed enums.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayoutOut",
      "field": "estimated_time_of_arrival",
      "reason": "Covers PayoutOut/PayoutOnEvmOut's inline tracking_payment.estimated_time_of_arrival and tracking_liquidity.estimated_time_of_arrival. Both are spec enums (5_min/5_30_min/30_min/2_hours/1_business_day/2_business_days/5_business_days); PayoutTrackingPayment and TrackingLiquidity model them as plain `?string`/`string`, not backed enums.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayoutOut",
      "field": "provider_status",
      "reason": "Covers PayoutOut/PayoutOnEvmOut's inline tracking_payment.provider_status and tracking_liquidity.provider_status. Both are spec enums; PayoutTrackingPayment and TrackingLiquidity model them as plain `?string`/`string`, not backed enums.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayoutOut",
      "field": "provider_name",
      "reason": "Covers PayoutOut/PayoutOnEvmOut's inline tracking_payment.provider_name, a spec enum of bank-provider display names. PayoutTrackingPayment models it as a plain `?string`, not a backed enum.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayoutOut",
      "field": "ach_cop_document_type",
      "reason": "Spec constrains ach_cop_document_type to an enum. Payout models it as a plain `?string`, not the AchCopDocument backed enum that BankAccounts already uses on the same rail; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "VirtualAccountOut",
      "field": "kyc_status",
      "reason": "Same shape of divergence as CustomerOut.kyc_status above: spec constrains kyc_status to an enum, VirtualAccount models it as a plain `?string`, not a backed enum. Every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "VirtualAccountOut",
      "field": "account_type",
      "reason": "Spec constrains us.account_type to an enum (checking/saving). VirtualAccountUsDetails models it as a plain `string`, not BankAccountType; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "WebhookEndpoint",
      "field": "events",
      "reason": "Spec constrains each item of the events array to the webhook-topic enum (WebhookEvents, already mapped). WebhookEndpoint models the whole array as plain `?array`, not a typed list of WebhookEvents; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "WebhookEndpointIn",
      "field": "events",
      "reason": "Same divergence as WebhookEndpoint.events, on the create-input side (CreateWebhookEndpointInput).",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "PayinOut",
      "field": "provider_name",
      "reason": "Covers PayinOut/CreatePayinOut's inline tracking_transaction.provider_name, a spec enum of payment-provider names. TrackingTransaction models it as a plain `?string`, not a backed enum; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    },
    {
      "schema": "CreatePayinQuoteIn",
      "field": "pse_document_type",
      "reason": "Spec constrains payer_rules.pse_document_type to a 2-value enum (CC/NIT). PayerRules models it as a plain `?string`; every spec value still parses without error.",
      "owner": "eric@blindpay.com"
    }
  ]
}
